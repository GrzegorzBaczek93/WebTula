import React from 'react';

import '@styles/global.css';
import Layout from '@components/common/layout';
import Form from '@components/comissions/form';

const ComissionsPage = () => {
    return (
        <Layout>
            <Form />
        </Layout>
    );
};

export default ComissionsPage;
